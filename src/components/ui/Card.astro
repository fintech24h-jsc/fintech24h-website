---
// src/components/ui/Card.astro
interface Props {
  class?: string;
  id?: string;
  [key: string]: any;
}

const { class: className, id, ...rest } = Astro.props;
---

<div class:list={["card-default p-6 sm:p-8", className]} id={id} {...rest}>
  <slot />
</div>
